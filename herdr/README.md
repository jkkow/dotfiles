# Herdr

Terminal workspace manager configuration. `config.toml` is tracked; Herdr
runtime artifacts remain ignored.

## Configure

After changing `config.toml`, reload the running Herdr server:

```powershell
herdr server reload-config
```

New interactive panes use PowerShell 7 (`pwsh`) by default.
